[project]
name = "gibdd-checker"
version = "0.1.0"
description = "Автоматический чекер свободных слотов ГИБДД через Госуслуги"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.1",
    "pyotp>=2.10.0",
    "selenium>=4.20.0",
    "webdriver-manager>=4.0.0",
]